# Scan-PythonPackages

PowerShell static security scanner for Python packages submitted through a media transfer review workflow. It supports operator-driven inspection of untrusted Python artifacts before they are admitted to a trusted environment.

This repository is being migrated to a canonical `src/`, `tests/`, and `docs/` layout. See `CHANGELOG.md` for release history.

